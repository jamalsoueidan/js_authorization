<% js_form_for :user, @user do |f| %>
	<%= f.password_field :password %>
	<%= f.password_field :password_confirmation %>
	<%= f.hidden_field :id, :value => params[:id]%>
	<%= f.submit "Opret nyt adgangskode" %>
<% end %>
