<% js_form_for :user do |f| %>
	<%= f.text_field :email %>
	<%= f.password_field :password %>
	<%= f.submit_or_cancel :submit => {:label => tt("user.login.button_submit")}, :cancel => {:label => tt("user.login.button_forgot_password"), :url => {:action => "forgot_password"}} %>
<% end %>